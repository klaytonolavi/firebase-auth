import React, { Component } from 'react'

export default class Home extends Component {
  render () {
    return (
      <div>
        Home Page which is not protected. Anyone can see this page if not signed in. 
      </div>
    )
  }
}